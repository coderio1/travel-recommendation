# Dynamic TLS configuration
tls:
  certificates:
    - certFile: /etc/traefik/cert/cert.pem
      keyFile: /etc/traefik/cert/key.pem
  stores:
    default:
      defaultCertificate:
        certFile: /etc/traefik/cert/cert.pem
        keyFile: /etc/traefik/cert/key.pem

