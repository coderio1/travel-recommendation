# Dynamic TLS configuration
# No static certificates needed — Let's Encrypt certs are managed automatically
# by the ACME resolvers defined in traefik.yml. Each router uses:
#   traefik.http.routers.<name>.tls.certresolver=letsencrypt
tls:
  stores:
    default: {}
